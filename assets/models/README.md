# MediaPipe model asset

Place the official MediaPipe Hand Landmarker model at:

`assets/models/hand_landmarker.task`

The application never downloads a model at runtime. See the repository README
for the official download page and setup command.

